{
    /**
    * include用来指定哪些ts文件需要编译
    * 路径: ** 代码任意目录，  * 代码任意文件
    * exclude默认值: ['node_modules', 'bower_components', 'jspm_packages']
    */
    // "include": [
    //     "./src/**/*"
    // ],
    // 编译器选项
    // "compilerOptions": {
    //     "target": "ES6",
    //     "module": "ES6",
        // "lib": ["ES2015", "DOM"],
        // "outDir": "./dist",
        // 将代码合并成一个文件
        // "outFile": "./dist/app.ts"
        
        // 是否对js文件编译，默认false
        // "allowJs": false,

        // 是否检查js代码语法规范，默认false
        // "checkJs": false,

        // 是否删除注释
        // "removeComments": true,

        // 不生成编译后的文件,默认false
        // "noEmit": false,

        // 当错误时不生成编译后的文件
        // "noEmitOnError": true,

        // 所有严格检查的总开关
        // "strict": true,

        // // 设置编译后的文件是否使用严格模式，默认false
        // "alwaysStrict": true,

        // // 不允许隐式any类型
        // "noImplicitAny": true,

        // // 不允许不明确类型的this
        // "noImplicitThis": true,

        // // 严格的检查空值
        // "strictNullChecks": true,

        
    // }
    "compilerOptions": {
        "module": "ES2015",
        "target": "ES2015",
        "strict": true,
        "noEmitOnError": true
    }
}